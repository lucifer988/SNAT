# SNAT Agent 公网直连 —— Caddy TLS 终止示例（仅供“独立加密链路”参考）
#
# 重要说明：当前面板代码对 Agent URL 仍固定使用 `http://{host}:{port}`，因此不能把
# “服务器地址=域名，端口=443” 直接当成 HTTPS Agent 来接入；否则面板实际会请求
# `http://agent.your-domain.com:443/...`，这与标准 HTTPS 不一致。
#
# 所以这个示例的用途仅限于：
#   1) 你自己手动验证/调试 Agent 的 TLS 入口；或
#   2) 未来若面板增加可配置 scheme（http/https）字段时，作为参考模板。
#
# 对当前版本，生产上仍推荐：
#   * 首选 WireGuard / 内网模式；或
#   * Agent 继续用 HTTP，但只监听内网/WG 地址，不暴露公网。
#
# 如果你只是想给 Agent 加一层公网 TLS 入口，下面的 Caddy 配置本身是成立的：
# 它会把公网 443 反代到本机 127.0.0.1:8888，并且只允许面板出口 IP 访问。
# 但请注意：当前 SNAT 面板还不能把这一路径当作正式 Agent 接入地址。
#
# 部署步骤：
#   1) Agent 只监听本机回环：把 systemd 里 AGENT_HOST 改成 127.0.0.1，并重启 snat-agent。
#        sudo sed -i 's/AGENT_HOST=.*/Environment="AGENT_HOST=127.0.0.1"/' /etc/systemd/system/snat-agent.service
#        sudo systemctl daemon-reload && sudo systemctl restart snat-agent
#   2) 装 Caddy：https://caddyserver.com/docs/install
#   3) 本文件写进 /etc/caddy/Caddyfile（域名换成解析到本机的域名），reload caddy。
#   4) 云防火墙放行 443（并关闭 8888 的公网放行）。
#
# 额外加固：把 @panel 的来源 IP 限制成面板出口 IP，双重保险（Agent 侧 AGENT_ALLOWED_IPS 亦可）。

agent.your-domain.com {
    encode gzip

    # 只放行面板出口 IP（把 203.0.113.10 换成你面板服务器的公网出口 IP）
    @panel {
        remote_ip 203.0.113.10
    }
    handle @panel {
        reverse_proxy 127.0.0.1:8888
    }
    # 其它来源一律拒绝
    handle {
        respond "Forbidden" 403
    }

    log {
        output file /var/log/caddy/snat-agent.log
    }
}
